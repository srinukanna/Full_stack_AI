### git concepts
# -> heading
* -> bullet point
```
 -> consider the content is present inside the grey box which can be able to copy by anyone
.
-> the backticks usually used to storing or representing the codes irrespective of any language
```

# git essentials

* first we have to install git
* git is a version control tool
* it shows the changes whatever we done in the code  irrespective of  any time.
* git acts as a tool and github acts as cloud storage

### local machine (laptop)

#### General terms

 * local repository 
 ```
general which we created on the desktop or in any laptop memory location.
```

### Remote machine
it is nothing about representing about github

#### general terms
* Remote repository
```
it is nothing but the repository or the folder which we create on the github 

```

* for uploading or updating the code to github ,we must connect the local repository to remote repository

* then after linking the our local repository to remote repository using git tool we can update the changes further to github(remote environment).

* for linking local repository to remote repository we must use some git commands.

#### step-1
* we need to create local repository in our laptop using terminal or user choice.
* here using the terminal as follows

```
mkdir git notes
```

#### step-2
* go to github and click the create new repository option.

```





